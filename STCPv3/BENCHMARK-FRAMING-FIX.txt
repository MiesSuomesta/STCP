STCP tcp-path benchmark framing overlay v5

Scope: tests/tcp-path/stcp_tcp_path_bench.c only.

Fixes TCP/STCP byte-stream framing in the benchmark:
- send_msg loops across short send()
- recv_exact_msg loops until full control/ack structure
- server MODE_TX drains exactly chunk*count bytes
- client MODE_RX drains exactly chunk*count bytes
- call counters represent actual recv() calls

No kernel/core/carrier/SDK/protocol changes.

Build:
  cc -O2 -Wall -Wextra -o stcp_tcp_path_bench stcp_tcp_path_bench.c
