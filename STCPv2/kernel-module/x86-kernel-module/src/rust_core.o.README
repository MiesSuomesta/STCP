Place the combined Rust object here as src/rust_core.o.

Build example:

RUSTFLAGS="-C relocation-model=static -C panic=abort" \
cargo +nightly build -Zbuild-std=core,alloc \
    --target x86_64-unknown-none --release
