STCPv2 Zephyr RX-ready rollback

Restores the three files changed by stcp-zephyr-rx-ready-overlay.zip
back to the baseline from stcp-25082026_084116.zip.

Apply over the STCP root directory, then do a clean build and flash.

This rollback intentionally contains no new functional change.
